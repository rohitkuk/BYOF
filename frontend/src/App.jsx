export default function App() {
  return (
    <div style={{
      height: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: 'var(--bg)',
    }}>
      <h1 style={{
        fontFamily: "'Playfair Display', Georgia, serif",
        color: 'var(--primary)',
        fontSize: '40px',
        fontWeight: 700,
        letterSpacing: '0.1em',
      }}>
        BYOF
      </h1>
    </div>
  )
}
